services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE:-team_resource}
      POSTGRES_USER: ${DB_USERNAME:-app}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-app} -d ${DB_DATABASE:-team_resource}"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_URL: http://localhost:8080
      FRONTEND_URL: http://localhost:8080
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-team_resource}
      DB_USERNAME: ${DB_USERNAME:-app}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      SESSION_DRIVER: database
      SESSION_DOMAIN: localhost
      SESSION_SAME_SITE: lax
      SANCTUM_STATEFUL_DOMAINS: localhost:8080,127.0.0.1:8080
      CORS_ALLOWED_ORIGINS: http://localhost:8080
      # 招待メール (TODO-3) 用に mailpit へ。 web UI: http://localhost:8025
      MAIL_MAILER: smtp
      MAIL_HOST: mailpit
      MAIL_PORT: 1025
      MAIL_FROM_ADDRESS: noreply@example.com
      MAIL_FROM_NAME: Team Resource Dashboard
    volumes:
      - ./backend:/var/www/html
    expose:
      - "8000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8080
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    expose:
      - "3000"

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    depends_on:
      - backend
      - frontend
    ports:
      - "8080:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

  # Mailpit: 開発用 SMTP サーバ + Web UI で送信メールを目視確認できる。
  # SMTP: 1025 / Web: http://localhost:8025
  # 招待メール (TODO-3) を含む全メールを captures し、 production には決して接続しない。
  mailpit:
    image: axllent/mailpit:v1
    restart: unless-stopped
    ports:
      - "8025:8025"
    expose:
      - "1025"

volumes:
  postgres_data:
